{
  "name": "api-gateway",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16",
    "express": "^4",
    "express-http-proxy": "^1",
    "jsonwebtoken": "^9"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
